services:
  vital:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      DATABASE_URL: "file:/app/data/vital.db"
    volumes:
      - vital_data:/app/data

volumes:
  vital_data:
    name: vital_data
